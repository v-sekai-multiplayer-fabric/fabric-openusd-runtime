# fabric-openusd-runtime
Prebuilt, per-triplet OpenUSD runtime binaries (v26.05) for the fabric-flow-openusd hexagon, with an Elixir/Hex resolver. Built once per platform so adapters and Elixir consumers fetch instead of recompiling USD.
